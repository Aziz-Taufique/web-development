const math = require("./math"); // checking in current directory

// math => gives an objext

// const math = require("fs");   ==>  Checking the existing directory in node
// const math = (require = "https");  ==> Checkig the existing directory in node

console.log(math); // it gives an object

console.log(`add vlaue is ${math.addVal(3, 5)} and sub val is ${(5, 3)}`);
